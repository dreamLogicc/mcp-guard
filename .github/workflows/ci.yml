name: CI

on:
  push:
    branches: ["**"]
    tags: ["v*"]
  pull_request:
    branches: ["**"]
    types: [opened, synchronize, reopened, ready_for_review]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12", "3.13"]

    steps:
      - uses: actions/checkout@v5

      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Install
        run: uv sync --all-groups --python ${{ matrix.python-version }}

      - name: Lint
        run: uv run ruff check .

      - name: Format check
        run: uv run ruff format --check .

      - name: Test
        run: uv run pytest --cov=praxiom --cov-report=term-missing

  package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Build
        run: uv build
      - name: Check metadata
        run: uvx twine check --strict dist/*

      - name: Install the wheel and run the CLI from outside the source tree
        run: |
          uv venv /tmp/probe
          VIRTUAL_ENV=/tmp/probe uv pip install dist/*.whl
          cd /tmp && /tmp/probe/bin/praxiom --help > /dev/null
          cd /tmp && /tmp/probe/bin/python -c "import praxiom; print(praxiom.__all__)"

      - uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/
